<!-- workspace-memory:start -->
## Workspace memory

Long-term memory for this repo lives in the hub workspace `{{HUB}}` under `memory/projects/{{NAME}}/`. Read its `CURRENT.md` and `ACTIVE_THREADS.md` at session start; search older context with `{{SEARCH}} "words"`. Record durable outcomes with:

```bash
{{CAPTURE}} checkpoint --project {{NAME}} --state "..." --next "..." --decision "title | choice | why"
```

Never commit memory into this repository; the hub commits it.
<!-- workspace-memory:end -->
